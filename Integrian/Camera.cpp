#include "IntegrianPCH.h"
#include "Camera.h"

Integrian::Camera::Camera(const uint32_t windowWidth, const uint32_t windowHeight)
	: m_WindowWidth{ windowWidth }
	, m_WindowHeight{ windowHeight }
{
}
